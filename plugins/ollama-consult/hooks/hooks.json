{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "VENV_DIR=\"${CLAUDE_PLUGIN_ROOT}/mcp-server/.venv\"; if [ ! -d \"$VENV_DIR\" ]; then python3 -m venv \"$VENV_DIR\"; fi; \"$VENV_DIR/bin/python3\" -m pip install -q -r \"${CLAUDE_PLUGIN_ROOT}/mcp-server/requirements.txt\" > /dev/null 2>&1 || true; echo '{}'",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
